# PyTorch DDP

Simple script demonstrating how to use PyTorch DDP to train a model on multiple GPUs and nodes of Aurora and Polaris.

To run on Polaris:

```cli
$ qsub sub_polaris.sh
```

To run on Aurora:

```cli
$ qsub sub_aurora.sh
```

